{
  "name": "navigation-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/init-storage.js && next dev",
    "build": "node scripts/init-storage.js && next build",
    "start": "node scripts/init-storage.js && next start",
    "lint": "next lint",
    "init": "node scripts/init-storage.js"
  },
  "dependencies": {
    "next": "14.0.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.3.6"
  }
}